import * as cdk from 'aws-cdk-lib';
import { Stack, StackProps } from 'aws-cdk-lib';
export declare class EksIstioStack extends Stack {
    constructor(scope: cdk.App, id: string, props?: StackProps);
}
